# 活字排版工坊

一个纯前端本地应用，用来维护字模库、在网格版面中落字、检查字模数量是否超用、保存草稿并导出PNG预览图。

- 版面可存为命名作品，作品列表支持预览、恢复、重命名与移除，与草稿分开管理。
- 落字、替换、清空、载入草稿均可撤销/重做（Ctrl/⌘+Z、Ctrl/⌘+Shift+Z），新编辑会清空重做栈。
- 保存作品或导出图片前自动核对字模用量，超量时按字模列出明细并拦截操作。
- 全部数据保存在浏览器 localStorage，无后端依赖。

直接打开 `index.html` 即可使用，也可以在本目录启动静态服务访问。
